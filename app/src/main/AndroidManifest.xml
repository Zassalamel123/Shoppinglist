<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.einkaufslisteapp">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Einkaufsliste">
        <activity
                android:name=".Activities.ListEditActivity"
                android:parentActivityName=".Activities.ManagerListActivity">
        </activity>
        <activity
                android:name=".Activities.ListCreationActivity"
                android:parentActivityName=".Activities.ManagerListActivity">
        </activity>
        <activity
                android:name=".Activities.ManagerListActivity"
                android:parentActivityName=".Activities.MainActivity"/>
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>